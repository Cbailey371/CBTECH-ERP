# Configuración del servidor ERP
NODE_ENV=production
PORT=5001
# Reemplazar por una cadena aleatoria larga (ej: openssl rand -base64 32)
JWT_SECRET=reemplazar_por_cadena_aleatoria_larga_y_segura
JWT_EXPIRATION=24h

# Base de datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=erp_db
DB_USER=admin_user
DB_PASSWORD=password_seguro
DB_DIALECT=postgres

# Seguridad
CORS_ORIGIN=https://tu-dominio-frontend.com
FRONTEND_URL=https://tu-dominio-frontend.com
